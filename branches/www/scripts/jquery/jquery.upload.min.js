$.upload = function(params){
	var options = {
		id      : 0
	};
	$.extend(options, params);

	tb_show('选择图片文件', '/gallery/goods/id/' + options.id + '?keepThis=true&TB_iframe=true&height=500&width=800', false);
};